import { normalize, colors } from 'react-native-elements';

const primary = colors.primary || '#5fb8f6';

export default {
  primary: primary,
  headerIcon: primary,
  checkedIcon: primary,
  uncheckedIcon: colors.grey0,
};
